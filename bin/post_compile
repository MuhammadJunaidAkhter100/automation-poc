#!/usr/bin/env bash
# Heroku post-compile hook: install Playwright browsers matched to the Python package version.
# Runs after pip install, before the app starts.
playwright install chromium --with-deps 2>&1 | tail -5
